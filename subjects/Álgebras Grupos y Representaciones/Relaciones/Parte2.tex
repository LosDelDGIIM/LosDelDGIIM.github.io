\begin{ejerciciostarstar}\label{ej25}
    Supongamos $T:V\to V$ un endomorfismo $K$-lineal, donde $V$ es un espacio vectorial de dimensión finita que consideramos, como de costumbre, como un $K[x]$-módulo. Supongamos que el polinomio mínimo $m(x)$ de $T$ es irreducible en $K[x]$. Demostrar que existen $K[x]$-submódulos simples $V_1\ldots, V_t$ tales que $V = V_1\oplus \cdots\oplus V_t$ como $K[x]$-módulo.
\end{ejerciciostarstar}

\begin{ejerciciostarstar}
    En las condiciones del Ejercicio~\ref{ej25}, demostrar que el polinomio característico de $T$ es ${(m(x))}^{t}$. 
\end{ejerciciostarstar}

\begin{ejercicio}
    Encontrar todos los idempotentes en $\mathbb{Z}[\sqrt{3}]/\langle 3 \rangle $. \\
    
    \noindent
    \textbf{Solución:}
    Para encontrar los idempotentes, tomamos un elemento genérico, lo multiplicaremos por sí mismo y lo igualaremos al mismo:
    Sea $a+b\sqrt{3} \in \mathbb{Z}[\sqrt{3}]/\langle 3 \rangle $.
    \begin{equation*}
        (a+b\sqrt{3})^{2} = a^2 + 2ab\sqrt{3} = a + b\sqrt{3} \Longleftrightarrow a \in \{0,1\} \land b = 0
    \end{equation*}
    En definitiva,  los únicos idempotentes son el $0$ y el $1$.
\end{ejercicio}

\begin{ejerciciostarstar}
    Sea $R$ un álgebra sobre un cuerpo de característica distinta de 2, y $a,b,e \in R$ idempotentes. Demostrar que si $e=a+b$, entonces $ab=ba=0$. Si la característica es 2, encontrar un contraejemplo con $b\neq a$. 
\end{ejerciciostarstar}

\begin{ejercicio}
    Dar un CCIO con $n$ elementos para $R=M_n(K)$. \\
    
    \noindent
    \textbf{Solución:}
    \begin{equation*}
        e_i = (a_{kl})
    \end{equation*}
    donde $a_{kl} = 1$ si $k=l=i $, y $a_{kl}=0$ en cualquier otro caso. Es evidente que $e_1 + \cdots + e_n = I_n$, es fácil comprobar que $e_i e_i = e_i $ y también que $e_ie_j = 0$ si $i \neq j$. 
\end{ejercicio}

\begin{ejercicio}
    Supongamos que $M$ es un módulo sobre un anillo $A$. Entonces $End_A(M)$ es un subanillo de $End(M)$, llamado \textit{anillo de endomorfismos del} $A$\textit{-módulo M} (y es una subálgebra de $End_K(M)$ si $A$ es una $K$-álgebra). Si $M = M_1 \oplus \cdots \oplus M_n$, denotemos, para cada $i=1\ldots n$, por $\iota_i:M_i \to M$ la inclusión canónica, y por $\pi_i:M \to M_i $ la aplicación que asigna $\pi_i(m) = m_i $ a cada $m = m_1 + \cdots + m_n \in M$, con $m_j \in M_j$ para $j = 1\ldots n$. Si denotamos por $e_i = \iota_i\pi_i $ para $i=1\ldots n$, no es difícil ver que $\{e_1, \ldots, e_n\}$ es un CCIO para el anillo $End_A(M)$. Comprobar todas las afirmaciones. \\
    
    \noindent
    \textbf{Solución:}
    
\end{ejercicio}

\begin{ejercicio}
    Sea $M$ un $A$-módulo, y pongamos $R=End_A(M)$. Supongamos que $M=M_1 \oplus \cdots \oplus M_n$ para submódulos $M_i $ tales que existe un $A$-módulo $N$ y un isomorfismo de $A$-módulos $M_i \cong N$ para cada $i=1\ldots n$. Si $\Delta = End_A(N)$, entonces exxiste un isomorfismo de anillos $R\cong M_n(\Delta)$ que, si $A$ es una $K$-álgebra, es de $K$-álgebras. Cubrir los detalles respecto a los apuntes. 
\end{ejercicio}

\begin{ejercicio}
    Sea $\{e_1,\ldots,e_n\}$ un CCIO para $R$. Demostrar que los idempotentes $e_1,\ldots,e_n$ son centrales si, y solo si, $e_iRe_j = \{0\}$ para todo $i\neq j$. \\
    
    \noindent
    \textbf{Solución:}
    \begin{description}
        \item [$\Longrightarrow$)] $e_ire_j = re_ie_j = 0$.
        \item [$\Longleftarrow$)] $e_ir = e_ir1= e_ir\sum_{}^{}e_j = \sum_{}^{}e_ire_j \AstIg e_ire_i \AstIg (\sum_{}^{}e_j)re_i = 1re_i = re_i$
    \end{description}
    Donde en $(\ast)$ hemos usado la hipótesis.
\end{ejercicio}

\begin{ejercicio}\label{ej33}
    Demostrar que, si $M$ un $R$-módulo y $e\in R$ un idempotente central, entonces $eM = \{em : m\in M\}$ es un submódulo de $M$. Si ahora $\{e_1,\ldots,e_n\}$ es un CCIO centrales de $R$, demostrar que $M = e_1M \oplus \cdots \oplus e_nM$. \\
    
    \noindent
    \textbf{Solución:}
    Para ver que $eM$ es un submódulo, debemos comprobar que es subgrupo aditivo y es cerrado para producto por escalares por la izquierda.
    \begin{itemize}
        \item Sea $m, n \in M$: $em - en = e(m-n) \in eM$.
        \item Sea $m\in M, r \in R$: $rem \AstIg erm \in eM$, donde en $(\ast)$ hemos usado la hipótesis y la última pertenencia se debe a que $M$ es un $R$-módulo.
    \end{itemize}

    Para la segunda afirmación, observamos que $m=1\cdot m = (e_1 + \ldots + e_n)\cdot m = e_1m+\ldots+e_nm$, por tanto, es claro que $M = \sum e_iM$. Para ver que son independientes, sea $m \in e_iM \cap \sum\limits_{i\neq j} e_jM$, entonces existen $m_1,\ldots,m_n$ tales que $e_im_i = \sum\limits_{i\neq j} e_jm_j$, pero $e_ie_im_i = e_im_i$, luego $e_im_i = e_i\sum\limits_{i\neq j} e_jm_j = \sum\limits_{i\neq j} e_ie_jm_j = 0$ por la ortogonalidad. Por tanto, $m=0$ y soon independientes. 
\end{ejercicio}

\begin{ejercicio}
    Con la notación del Ejercicio~\ref{ej33}, demostrar que $M$ es cíclico si, y solo si, $e_iM$ es cíclico para todo $i=1\ldots n$. \\
    
    \noindent
    \textbf{Solución:}
    \begin{description}
        \item [$\Longrightarrow$)] Supongamos que $M$ es cíclico, entonces existe $m\in M$ tal que $M=Rm$. Entonces $e_iM = e_iRm \AstIg Re_im = Rm'$, por tanto, $e_iM$ es cíclico.
        \item [$\Longleftarrow$)] Para cada $i \in \Delta_n$ $e_iM = Rm_i$ por hipótesis. Sea $m = m_1 + \ldots + m_n$, y nuestro objetivo será probar que $M=Rm$.\\
            Por el ejercicio~\ref{ej33} sabemos que $M = e_1M \oplus \ldots \oplus e_nM$. Sea pues $x\in M, x=x_1+\ldots+x_n$ con $x_i \in e_iM = Rm_i$ luego $x_i = r_im_i$. Sea ahora $r=r_1e_1 + \ldots + r_ne_n \in R$. Vamos a probar que $x=rm$ y así $x\in Rm$ y así probaremos que $M = Rm$. Observamos antes que $m_i = e_im' \Longrightarrow e_im_i = e_i^2m' = e_im' = m_i$
            \begin{gather*}
                rm = (r_1e_1+\ldots+r_ne_n)(m_1+\ldots+m_n) =\\
                \sum_{i,j} r_ie_im_j= \sum_{i,j}r_ie_ie_jm_j = \sum_i r_ie_im_i = \\
                \sum_i r_im_i = \sum x_i = x
            \end{gather*}
            Por tanto, $x\in Rm$ y $M$ es cíclico.
    \end{description}
\end{ejercicio}

\begin{ejerciciostar} \label{ej35}
    Consideramos un polinomio no constante $f \in K[x]$, con $K$ un cuerpo. Describir un CCIO (centrales) indescomponibles para $A=K[x]/\langle f \rangle $. \\
    
    \noindent
    \textbf{Solución:}
    Sea $f = \lm \prod\limits_{j=1}^{t} f_j^{p_j}$ factorización de $f$ en polinomios irreducibles mónicos. El Teorema Chino de los Restos, sabemos que existe el siguiente isomorfismo $\varphi $: 
    \begin{equation*}
        K[x]/\langle f \rangle \cong K[x]/\langle f_1^{p_1} \rangle \times \ldots \times K[x]/\langle f_t^{p_t} \rangle = A
    \end{equation*}

    Un CCIO para $A$ es el siguiente: $\{e_1, \ldots, e_t\}$, donde $e_j$ tiene todo ceros y un $1$ en la coordenada $j$-ésima. Está claro que esto define un CCIO.

    Pues un CCIO para $K[x]/\langle f \rangle $ es el siguiente: $\{\varphi^{-1}(e_1), \ldots, \varphi^{-1}(e_t)\}$. Comprobemos que es efectivamente un CCIO:
    \begin{itemize}
        \item $\varphi^{-1}(e_1) + \ldots + \varphi^{-1}(e_t) = \varphi^{-1}(e_1+\ldots+e_t) = \varphi^{-1}(1) = 1$.
        \item $\varphi^{-1}(e_i) \varphi^{-1}(e_i) = \varphi^{-1}(e_ie_i) = \varphi^{-1}(e_i)$
        \item $\varphi^{-1}(e_i) \varphi^{-1}(e_j) = \varphi^{-1}(e_ie_j) = \varphi^{-1}(0) = 0$
    \end{itemize}
\end{ejerciciostar}

\begin{ejerciciostarstar}
    Sea $V$ un $K$-espacio vectorial de dimensión finita $n$ y $T:V \to V$ una aplicación lineal. Diremos que un vector $v \in V$ es cíclico para $T$ si $\{v, T(v), \ldots, T^{n-1}(v)\}$ es una base de $V$ como $K$-espacio vectorial. Demostrar que $V$ admite un vector cíclico si, y solo si, el polinomio mínimo de $T$ tiene grado $n$. ¿Cuál es entonces la longitud de $V$ en tanto que $K[x]$-módulo?
\end{ejerciciostarstar}

\begin{ejercicio}
    Sea $A = K[x]/\langle x^2 \rangle $, donde $K$ es un cuerpo, y $K[x]$ es el anillo de polinomios. Demostrar que, visto como $A$-modulo, $A$ no tiene sumandos directos no triviales. \\
    
    \noindent
    \textbf{Solución:}
    Por el ejercicio~\ref{ej35} sabemos que un CCIO indesponibles es el formado por un único elemento, ya que por el Teorema Chino de los Restos, $A$ se queda igual. Por tanto, por un resultado que tenemos en la teoría, $Ae = A$ no se puede descomponer como suma directa de dos ideales no nulos. Es decir, $A$ no tiene sumandos directos no triviales.
\end{ejercicio}

\begin{ejercicio}
    Sean $M$ y $N$ módulos semisimples con descomposiciones como suma directa de submódulos simples $M = S_1\oplus \ldots\oplus S_t$ y $N=T_1\oplus \ldots\oplus T_s$. Supongamos que $S_i$ no es isomorfo a $T_j$ para todo $i = 1,\ldots,t,j=1,\ldots,s$. Demostrar que todo homomorfismo de módulos de $M$ a $N$ es cero. \\
    
    \noindent
    \textbf{Solución:}
    Sea $f:M \to N$ homomorfismo de módulos no nulo. Entonces, existe al menos un $S_i $ tal que $f:S_i \to N$ no nulo. Luego, existe al menos un $j \in \Delta_s$ tal que $\pi_j \circ f: S_i \to T_j$ es un homomorfismo de módulos simples no nulo, luego, por el Lema de Schur, es un isomorfismo. Pero esto es una contradicción con la hipótesis de que $S_i $ no es isomorfo a $T_j$, por lo que $f$ debe ser nula. Concluimos así que todo homomorfismo entre $M$ y $N$ es nulo.
\end{ejercicio}

\begin{ejercicio}\label{ej:39}
    Sea $M$ un módulo semisimple de dimensión finita con estructura $(\Sigma_1,n_1), \ldots, (\Sigma_t,n_t)$. Si $N$ es un submódulo de $M$, demostrar que su estructura es $(\Sigma_1,m_1), \ldots, (\Sigma_t,m_t)$ para ciertos $m_j\leq n_j$ (admitimos que $m_j=0$ significa que $\Sigma_j$ no aparece en la estructura de $N$). \\
    
    \noindent
    \textbf{Solución:}
    Como $M$ es semisimple, entonces $\exists X \in \cc{L}(M)$ tal que $M = N \oplus X$. Por un resultado de la teoría, $N,X$ son también semisimples. Es decir, $N = N_1 \oplus \ldots \oplus N_s$ y $X = X_1 \oplus \ldots\oplus X_r$. Por tanto, $M = N_1 \oplus \ldots \oplus N_s \oplus X_1 \oplus \ldots\oplus X_r$. Sabemos que esta descomposición es única, es la razón por la que le hemos dado el nombre de estructura de $M$, luego para cada $i \in \Delta_s$ tenemos que $N_i \cong \Sigma_j$ para algún $j \in \Delta_t$. Y de aquí se deduce lo pedido.
\end{ejercicio}

\begin{ejercicio}
    Establecer un enunciado análogo al del Ejercicio~\ref{ej:39} para cada cociente de $M$. 
\end{ejercicio}


\begin{ejercicio}
    Dada una $K-$álgebra $A$, demostrar que la aplicación $\rho:A\to {(End_A(A))}^{op}$ definida por $\rho(a)(a') = a'a$ es un isomorfismo de $K-$álgebras.\\
    
    \noindent
    \textbf{Solución:}
    Aquí debemos comprobar 3 cosas: que la aplicación está bien definida, que es un homomorfismo de $K$-álgebras y que es biyectiva. \\

    Empecemos probando que está bien definida. Debemos probar que dado $a \in A$, $\rho(a) \in End_A(A)$. 
    \begin{itemize}
        \item $\rho(a)(b + c) = (b+c)a = ba + ca = \rho(a)(b) + \rho(a)(c)$
        \item $\rho(a)(bc) = (bc)a = b(ca) = b \rho(a)(c)$
    \end{itemize}
Veamos ahora que es un homomorfismo de $K$-álgebras. 
\begin{itemize}
    \item $\rho(a+b)(c) = c(a+b) = ca + cb = \rho(a)(c) + \rho(b)(c)$
    \item $\rho(ka)(c) = c(ka) = k(ca) = k\rho(a)(c)$
    \item $\rho(ab)(c)= c(ab) = (ca)b = \rho(a)(c)b = \rho(b)(\rho(a)(c)) = (\rho(b) \circ \rho(a))(c) = (\rho(a)\ast \rho(b))(c)$
\end{itemize}
Por último, veamos que es biyectiva:
\begin{itemize}
    \item Sea $\rho(a) = 0$, es decir, $0 = \rho(a)(1) = 1a = a$, por tanto, $\rho$ es inyectiva.
    \item Sea ahora $f \in End_A(A)$, llamamos $f(1) = a$. Así, $f(x) = xa = \rho(a)(x)$, para todo $x \in A$. Por tanto, $f$ es la imágen de $a$, luego es sobreyectiva.
\end{itemize}
\end{ejercicio}

\begin{ejerciciostar}
    Sea $B$ un álgebra. Demostrar que la aplicación que asigna a cada matriz su traspuesta da un isomorfismo de álgebras ${(M_n(B))}^{op} \cong M_n(B^{op})$. \\
    
    \noindent
    \textbf{Solución:}
    
\end{ejerciciostar}

\begin{ejercicio}
    Sea $\varphi:R\to S$ un isomorfismo de $K-$álgebras e $I,J$ ideales a izquierda de $R$. Demsotrar que $\varphi(I),\varphi(J)$ son ideales a izquierda de $S$ y que dado cualquier homomorfismo de $R-$módulos $f:I\to J$, la aplicación $\hat{f}:\varphi(I)\to \varphi(J)$ definida por $\hat{f}(y) = \varphi f \varphi^{-1}(y)$ para $y\in \varphi(I)$ es un homomorfismo de $S-$módulos. \\
    
    \noindent
    \textbf{Solución:}
    Primero demostramos que $\varphi(I)$ y $\varphi(J)$ son ideales a izquierda de $S$. Lo probamos para uno de los dos, pues el otro es análogo. Sea $x,y \in \varphi(I)$, entonces existen $x', y' \in I$ tales que $\varphi(x') = x, \varphi(y') = y$. Y como $I$ es un ideal por la izquierda pues: $x + y = \varphi(x') + \varphi(y') = \varphi(x'+y') \in \varphi(I)$. Por otro lado, dado $s\in S$ sea $r= \varphi^{-1}(s)$, $sx = \varphi(r)\varphi(x') = \varphi(rx') \in \varphi(I)$. \\

    Ahora debemos ver que $\hat{f}$ es $S$-lineal.
    \begin{itemize}
        \item Es claro que es un homomorfismo de grupos, pues es composición de homomorfismos de grupos.
        \item Sea $s\in S$. $\hat{f}(sy) = \varphi f \varphi^{-1}(sy) = \varphi f (\varphi^{-1}(s) \varphi^{-1}(y)) = \varphi(\varphi^{-1}(s)f(\varphi^{-1}(y))) = \varphi(\varphi^{-1}(s)) \varphi f \varphi^{-1}(y) = s \varphi f \varphi^{-1}(y)$
    \end{itemize}
\end{ejercicio}

\begin{ejercicio}
    Sean $R_1,\ldots,R_n$ $K-$álgebras y $R=R_1\times \ldots\times R_n$. Los ideales por la izquierda de $R$ son de la forma $I_1\times \ldots\times I_n$, con $I_i$ ideal a izquierda de $R_i$ para $i = 1,\ldots,n$. Análoga descripción tienen los ideales de $R$. \\
    
    \noindent
    \textbf{Solución:}
    Sea $I \in \cc{L}(M)$. Defino $I_i = \pi_i(I)$, siendo $\pi_i $ la proyección canónica de $R$ sobre $R_i $. Debemos comprobar ahora dos cosas: 
    \begin{itemize}
        \item Debemos ver que $I_i $ es un ideal por la izquierda de $R_i $. Sean $r_i \in R_i, x_i \in I_i $, existe pues un $(x_1,\ldots,x_i,\ldots,x_n)= x \in I$ tal que $\pi_i(x) = x_i$. Sea ahora $(0,\ldots,r_i,\ldots,0) \in R$, como $I$ es un ideal por la izquierda pues $rx \in I$, luego $r_ix_i = \pi_i(rx) \in I_i $.
        \item Debemos ver que $I = I_1 \times \ldots \times I_n$. Está claro que $I \subset I_1 \times \ldots\times I_n$, ya que estos se definen como proyecciones del primero. Para la otra inclusión, sea $(x_1,\ldots,x_n) \in I_1\times\ldots\times I_n$, razonando como antes, $(0,\ldots,x_i,\ldots,0) \in I$ y este es un subgrupo aditivo, por lo que $(x_1,\ldots,x_n) \in I$. 
    \end{itemize}
\end{ejercicio}

\begin{ejerciciostar}
    Sea $A$ un álgebra simple finito-dimensional. Demostrar que $R=M_n(A)$ es un álgebra simple de dimensión finita. Demostrar asimismo que si $\Sigma$ es un $A-$módulo simple y $M$ es un $R-$módulo simple, entonces $End_A(\Sigma)$ y $End_R(M)$ son álgebras de división isomorfas.
\end{ejerciciostar}

\begin{ejercicio}
    Sea $R$ un anillo y $\{e_1,\ldots,e_m\}$ un CCIO centrales de $R$. Sea $S$ un $R-$módulo simple. Demostrar que existe un único $j\in \{1,\ldots,m\}$ tal que $e_jS\neq \{0\}$. Deducir que, para $s\in S$; se tiene que $e_js = s$ y $e_is = 0$ para $i\neq j$. \\
    
    \noindent
    \textbf{Solución:}
    Sea $s \in S$, podemos escribir lo siguiente: $0 \neq s = 1s = (e_1+ \ldots+ e_m) s = e_1s + \ldots + e_ms$, luego si $e_jS = \{0\}$ para todo $j \in \Delta_m$, entonces $s=0$. Por tanto, existe al menos un $j$ que cumple. Para ver la unicidad, observemos que $e_jS \in \cc{L}(S)$ y como este es simple, $e_jS = S$. Entonces, dado $s \in S$ existe un $s' \in S$ tal que $s = e_js'$. Multiplicando ahora por $e_k$, $e_ks = e_ke_js' = 0$, luego $e_kS = \{0\}$. Por tanto, ya tenemos la unicidad. Es claro que $e_ks = 0$ para $k\neq j$ y para la otra afirmación, recordemos que $s = e_1s + \ldots + e_ms = e_js$.
\end{ejercicio}
