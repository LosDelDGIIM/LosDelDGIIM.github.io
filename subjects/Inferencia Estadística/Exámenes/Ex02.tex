\documentclass[12pt]{article}

\input{../../_assets/preambulo.tex}

\newcommand{\E}{\mathrm{E}}
\newcommand{\V}{\mathrm{Var}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\Rmas}{\R^{+}}
\newcommand{\X}{\mathcal{X}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\I}{\mathrm{Im}}
\newcommand{\Rmenos}{\R^{-}}

\begin{document}

    % 1. Foto de fondo
    % 2. Título
    % 3. Encabezado Izquierdo
    % 4. Color de fondo
    % 5. Coord x del titulo
    % 6. Coord y del titulo
    % 7. Fecha

    
    \input{../../_assets/portada}
    \portadaExamen{ffccA4.jpg}{Inferencia \\ Estadística\\Examen II}{Inferencia Estadística. Examen II}{MidnightBlue}{-8}{28}{2026}{}

    \begin{description}
        \item[Asignatura] Inferencia Estadística.
        \item[Curso Académico] 2022-23.
        \item[Grado] Grado en Matemáticas y Grado en Ingeniería Informática y Matemáticas.
        \item[Descripción] Examen Ordinario.
        % \item[Fecha] 23 de Enero de 2025.  
    \end{description}
    \newpage


    % ------------------------------------

    \begin{ejercicio}[2.25 puntos]
        Sea $(X_1,\ldots,X_n)$ una muestra aleatoria simple de una variable $X$ con distribución en una familia paramétrica.
        \begin{enumerate}[label=\alph*)]
            \item Dar la definición de estadístico suficiente. Enunciar el Teorema de Factorización de Neymann-Fisher. Demostrar dicho teorema para variables discretas.
            \item Si la función de distribución de $X$ es $F_\theta(x) = 1-e^{\theta-x}, x>\theta$, encontrar el intervalo de confianza para $\theta$ de menor longitud media uniformemente a nivel de confianza $1-\alpha$ basado en un estadístico suficiente.
        \end{enumerate}
    \end{ejercicio}

    \begin{ejercicio}[2.25 puntos]
        Sea $(X_1,\ldots,X_n)$ una muestra aleatoria simple de una variable con función de densidad:
        \begin{equation*}
            f_\theta(x) = \frac{1}{2\sqrt{2\theta-1}\sqrt{x-1}}, \qquad 1<x<2\theta
        \end{equation*}
        \begin{enumerate}[label=\alph*)]
            \item Sabiendo que $T = \max X_i$ es suficiente, encontrar, si existe, el UMVUE para ${(2\theta-1)}^{-1}$, especificando previamente el espacio paramétrico y el espacio muestral. Justificar la no existencia del UMVUE en los casos que corresponda.
            \item Calcular la función de verosimilitud y encontrar un estdimador máximo verosímil de $2\theta -1$.
        \end{enumerate}
    \end{ejercicio}

    \begin{ejercicio}[2 puntos]
        Sea $X$ una variable aleatoria con distribución en una famlia regular en el sentido de Fréchet-Cramér-Rao, cuyas funciones de densidad son de la forma:
        \begin{equation*}
            f_\theta(x) = exp\left[T(x)\ln\theta - \frac{\theta^2}{2} + S(x)\right], \qquad x\in R, \quad \theta \in \mathbb{R}^+
        \end{equation*}
        siendo $T(X)$ un estadístico regular.
        \begin{enumerate}[label=\alph*)]
            \item Calcular la función de información asociada a $X$.
            \item Basándose en una muestra aleatoria simple de $X$, $(X_1,\ldots,X_n)$, y suponiendo $T(X)>0$; encontrar la clase de funciones paramétricas que admiten estimador eficiente y los estimadores correspondientes.
            \item Bajo los supuestos del apartado $b)$, calcular la cota inferior para la varianza de los estimadores insesgados en $\ln\theta$, regulares, y justificar si se alcanza o no dicha cota.
        \end{enumerate}
    \end{ejercicio}

    \begin{ejercicio}[2.4 puntos]
        Contraste de hipótesis:
        \begin{enumerate}[label=\alph*)]
            \item Sea $(X_1,\ldots,X_n)$ una muestra aelatoria simple de una variable $X$ con distribución en una familia $\{P_\theta : \theta \in \Theta\}$. Sea $\Theta_0$ subconjunto arbitrario de $ \Theta$ y supongamos que se pretende contrastar la hipótesis $H_0:\theta \in \Theta_0$.
                \begin{enumerate}[label=\alph*)]
                    \item Detallar la hipótesis alternativa. Definir formalmente el concepto de test de hipótesis y dar la interpretación de sus valores.
                    \item Definir el tamaño y la función de potencia de un test arbitrario para resolver el problema anterior, explicando el significado de estos conceptos en término del rechazo de $H_0$.
                    \item En términos del tamaño y de la función potencia, ¿qué significa que un test tiene nivel de significación $\alpha$ para el problema de contraste planteado? ¿Cuáles son las condiciones para que un test sea UMP a nivel de significación $\alpha$?
                \end{enumerate}
            \item Obtener un test de razón de verosimilitud de tamaño $\alpha$ para contrastar $H_0:\theta\leq\theta_0$ frente a $H_1:\theta>\theta_0$, basado en una observación de una variable con la siguiente función de densidad (detallar y justificar todos los pasos para la obtención, incluyendo el estudio detallado del estadístico de contraste y su representación gráfica):
                \begin{equation*}
                    f_\theta(x) = \theta x^{-2}e^{\nicefrac{-\theta}{x}}, \qquad x>0
                \end{equation*}
                ¿Qué tamaños se alcanzan con dicho test?
        \end{enumerate}
    \end{ejercicio}

    % \noindent
    % \textbf{Solución.}
    % \newpage
    % \setcounter{ejercicio}{0}


\end{document}
